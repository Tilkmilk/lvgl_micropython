# 这是你 fork 后要用的 workflow（替换掉原 .github/workflows/build lvgl_mpy.yml 的内容）
# 改动只有一处：matrix.include 里只留小程这一个配置，其余原样保留
name: build many lvgl_micropython

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  build_esp32:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # ===== 小程 H1 专用配置 =====
          - board: ESP32_GENERIC_S3
            variant: SPIRAM_OCT        # 8MB Octal PSRAM
            flash_size: 16             # 16MB Flash
            display: st7789            # 2.0寸屏驱动（引脚在设备端 display.py 里配）
            indev: ""                  # 没有触摸屏，留空

    steps:
    - name: 1.检出 lvgl_micropython 主仓库（含全部子模块）
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: 2.安装 Ubuntu 系统级编译依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y \
        build-essential \
        pkg-config \
        cmake \
        ninja-build \
        ccache

    - name: 3.配置 Python 3.11 运行环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 4.按需浅克隆关键子模块（加速下载）
      run: |
        git submodule update --init --depth 1 -- lib/pycparser
        git submodule update --init --depth 1 --jobs 4 -- lib/micropython
        git submodule update --init --depth 1 --jobs 4 -- lib/lvgl

    - name: 5.加载或创建 ESP-IDF 缓存
      id: cache-deps
      uses: actions/cache@v4
      with:
        path: |
          lib/esp-idf
          ~/.espressif
        key: ${{ runner.os }}-v4-deps

    - name: 6.缓存未命中时完整拉取 ESP-IDF 及其子模块
      if: steps.cache-deps.outputs.cache-hit != 'true'
      run: |
        git submodule update --init --depth 1 --jobs 4 -- lib/esp-idf
        cd lib/esp-idf
        git submodule update --init --depth 1 --jobs 4 -- \
          components/bt/host/nimble/nimble \
          components/esp_wifi \
          components/esptool_py/esptool \
          components/lwip/lwip \
          components/mbedtls/mbedtls \
          components/bt/controller/lib_esp32 \
          components/bt/controller/lib_esp32c3_family
        cd ../..
        export "IDF_PATH=$GITHUB_WORKSPACE/lib/esp-idf"
        ./lib/esp-idf/install.sh all

    - name: 7.激活 ESP-IDF 环境变量（每个 step 都需重新执行）
      run: |
        export "IDF_PATH=$GITHUB_WORKSPACE/lib/esp-idf"
        . ./lib/esp-idf/export.sh
      env:
        IDF_PATH: ${{ github.workspace }}/lib/esp-idf

    - name: 8.修补组件依赖（移除多余的 esp_tinyusb，规避版本漂移导致的编译失败）
      run: |
        # micropython 自带 lib/tinyusb 子模块（hathach/tinyusb），并不使用
        # espressif/esp_tinyusb 组件；但 ports/esp32/main/idf_component.yml 里声明了
        # 对该组件的依赖（~1.0.0），组件注册表会拉到 1.0.3，其 usb_descriptors.c 引用了
        # tinyusb 0.21 已移除的宏 CFG_TUD_CDC_EP_BUFSIZE，导致 ninja 编译中断。
        # 由于 main 并不 REQUIRES 该组件，直接移除这条依赖即可。
        F=lib/micropython/ports/esp32/main/idf_component.yml
        sed -i '/^  espressif\/esp_tinyusb:/,+3d' "$F"
        rm -f lib/micropython/ports/esp32/dependencies.lock
        echo "--- patched $F ---"
        cat "$F"

    - name: 8b.补齐 TinyUSB 自定义 USB 描述符配置
      run: |
        # 当前 MicroPython 的 mp_usbd_descriptor.c 直接引用这两个 Kconfig 宏。
        # 直接写入 sdkconfig.base，避免 make.py 的额外参数解析丢失 CONFIG_ 项。
        S=lib/micropython/ports/esp32/boards/sdkconfig.base
        printf '\nCONFIG_TINYUSB_DESC_CUSTOM_VID=0x303A\nCONFIG_TINYUSB_DESC_CUSTOM_PID=0x4001\n' >> "$S"
        tail -n 6 "$S"

    - name: 9.使用 make.py 编译指定板型/Flash/显示/触摸配置
      run: |
        opts=()
        [[ -n "${{ matrix.variant }}" ]] && opts+=("BOARD_VARIANT=${{ matrix.variant }}")
        opts+=("BOARD=${{ matrix.board }}")
        opts+=("--flash-size=${{ matrix.flash_size }}")
        opts+=("DISPLAY=${{ matrix.display }}")
        [[ -n "${{ matrix.indev }}" ]] && opts+=("INDEV=${{ matrix.indev }}")
        python3 make.py esp32 "${opts[@]}"

    - name: 10.重命名并收集所有固件到 ~/artifacts
      run: |
        ARTIFACTS_DIR="$HOME/artifacts"
        mkdir -p "$ARTIFACTS_DIR"
        BUILD_DIR="${{ github.workspace }}/lib/micropython/ports/esp32"
        BOARD_NAME="${{ matrix.board }}"
        [[ -n "${{ matrix.variant }}" ]] && BOARD_NAME+="-${{ matrix.variant }}"
        FW_PATH="${BUILD_DIR}/build-${BOARD_NAME}/firmware.bin"

        OUT_NAME="lvgl_mpy_${{ matrix.board }}${{ matrix.variant && format('_{0}', matrix.variant) || '' }}_${{ matrix.flash_size }}MB${{ matrix.display && format('_{0}', matrix.display) || '' }}${{ matrix.indev && format('_{0}', matrix.indev) || '' }}.bin"

        if [[ -f "$FW_PATH" ]]; then
          cp "$FW_PATH" "$ARTIFACTS_DIR/$OUT_NAME"
        else
          echo "Firmware not found at $FW_PATH"
          exit 1
        fi

    - name: 11.上传重命名后的固件到 GitHub Actions 产物区
      uses: actions/upload-artifact@v4
      with:
        name: lvgl_mpy_${{ matrix.board }}${{ matrix.variant && format('_{0}', matrix.variant) || '' }}_${{ matrix.flash_size }}MB${{ matrix.display && format('_{0}', matrix.display) || '' }}${{ matrix.indev && format('_{0}', matrix.indev) || '' }}
        path: ~/artifacts/*.bin
